-- migrate.sql
-- Run this file to create the database/tables and seed products.
-- Recommended: run each file directly or run the migrate commands described in README.

-- If you want a single-file approach, you can concatenate the create_tables.sql and seed_products.sql contents into this file.

-- Example using mysql CLI (from repository root):
-- mysql -u root -p < server/db/sql/create_tables.sql
-- mysql -u root -p < server/db/sql/seed_products.sql

-- Or inside mysql client:
-- SOURCE server/db/sql/create_tables.sql;
-- SOURCE server/db/sql/seed_products.sql;
